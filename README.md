# 🧠 Personal Assistant Bot

CLI-додаток українською мовою для керування контактами та нотатками.  
Актуальна версія персонального помічника з повною підтримкою телефонів, адрес, email-ів, нотаток із тегами та збереженням даних між сесіями.

---

## ⚙️ Основні можливості

### 👥 Контакти
- Додавання, пошук, редагування, видалення контактів  
- Збереження імені, телефонів, email-ів, адрес, дня народження  
- Перевірка валідності номерів, email-ів і дат  
- Пошук за частиною імені або номером  
- Виведення контактів із днями народження за задану кількість днів  

### 🎂 Дні народження
- Збереження дня народження під час створення або редагування  
- Команда `show-birthday` — показати день народження контакту  
- Команда `birthdays [N]` — список привітань на найближчі _N_ днів  

### 📝 Нотатки та теги
- Створення, редагування, видалення нотаток, прив’язаних до контакту  
- Пошук нотаток за ключовими словами  
- Додавання тегів, пошук і сортування нотаток за тегами  
- Форматований вивід із датою створення  

### 💾 Збереження даних
- Контакти зберігаються до `addressbook.json`, нотатки — до `notebook.json`  
- Автоматичне завантаження даних при старті  
- Якщо файл пошкоджений або відсутній — створюється чиста адресна книга  

### 💬 Зручність CLI
- Фаззі-підказки (`difflib.get_close_matches`) при помилках у командах  
- Обробка винятків через декоратор `input_error`  
- Команди завершення: `exit`, `close`, `quit`, `good bye`  

---

## 📁 Структура проєкту

```
goit-python-final-project/
├─ README.md
├─ main.py                     # Точка входу CLI
└─ app/
   ├─ __init__.py
   ├─ config.py                # Команди, fuzzy-підказки, файли збереження
   ├─ decorators.py            # Обробка помилок
   ├─ entities.py              # AddressBook, Record, Note, теги
   ├─ exceptions.py            # Класи винятків
   ├─ handlers.py              # Логіка CLI-команд
   ├─ memory.py                # Збереження/завантаження JSON
   └─ utils.py                 # Парсинг вводу, допоміжні функції
```

---

## 🚀 Встановлення та запуск

```bash
git clone https://github.com/Reshekyr/goit-python-final-project.git
cd goit-python-final-project
python -m venv .venv
source .venv/bin/activate    # або .venv\Scripts\activate на Windows
python main.py
```

CLI автоматично завантажить попередні дані й запропонує ввести команду.

---

## 🧩 Команди CLI

| Команда | Призначення |
|---------|-------------|
| `hello` | Привітання та довідка |
| `add [name] [phone]` | Додати контакт або номер |
| `phone [name]` | Показати всі телефони |
| `all` | Усі контакти з повною інформацією |
| `search [рядок]` | Пошук за частиною імені чи номером |
| `change [name] [old] [new]` | Змінити номер |
| `delete [name]` | Видалити контакт |
| `add-email [name] [email]` | Додати email |
| `add-address [name] [адреса]` | Додати адресу |
| `add-birthday [name] [DD.MM.YYYY]` | Додати/оновити день народження |
| `show-birthday [name]` | Показати день народження |
| `birthdays [N]` | Найближчі дні народження |
| `add-note [name] [title] [text]` | Створити нотатку |
| `show-notes [name]` | Показати нотатки |
| `find-note [name] [keyword]` | Знайти нотатку |
| `edit-note [name] [title] [text]` | Редагувати нотатку |
| `delete-note [name] [title]` | Видалити нотатку |
| `add-tag [name] [title] [tag]` | Додати тег |
| `find-by-tag [name] [tag]` | Пошук за тегом |
| `show-tags [name]` | Показати теги |
| `sort-by-tags [name] [tag ...]` | Сортування за тегами |
| `exit`, `close`, `quit`, `good bye` | Завершення роботи |

---

## 💡 Автопідказки та валідація

- CLI пропонує схожі команди (`💡 Можливо: add, add-note?`)  
- `input_error` обробляє типові помилки без stack trace  
- Уся валідація (телефони, email-и, адреси, дати) зосереджена в `entities.py`
